class Order:

    def __init__(self, order_id, u_id, total_amount, order_date):
        self.order_id = order_id
        self.u_id = u_id
        self.total_amount = total_amount
        self.order_date = order_date
